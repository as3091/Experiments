; PlatformIO Project Configuration File
;
;   Build options: build flags, source filter
;   Upload options: custom upload port, speed and extra flags
;   Library options: dependencies, extra library storages
;   Advanced options: extra scripting
;
; Please visit documentation for the other options and examples
; https://docs.platformio.org/page/projectconf.html

[env:esp32-wroom-usbc]
platform = espressif32
board = esp32dev
framework = arduino
monitor_speed = 115200
upload_speed = 115200
board_build.f_flash = 80000000
board_build.flash_mode = qio

build_flags =
    -DPLATFORM_ESP32
    -DCEC_GPIO_PIN=4  ; As recommended in README for CEC line
    -Os

lib_deps =
    lucadentella/CEClient

build_src_filter =
    +<*>
    -<cec_controller/bkp_main.c>
    -<cec_controller/hal/cec_hal_linux.c>
    -<cec_controller/hal/gpio_hal_linux.c>

; ; ---- Listener mode: MQTT subscriber, waits for "send" signal ---------------
; [env:listener]
; platform = espressif32
; board = esp32dev
; framework = arduino
; monitor_speed = 115200
; upload_speed = 921600
; board_build.f_flash = 80000000
; board_build.flash_mode = qio
; lib_deps =
;     siara-cc/Sqlite3Esp32
;     knolleary/PubSubClient
; build_src_filter = +<*> -<main.cpp>
; extra_scripts = pre:load_env.py